/* eslint-disable @typescript-eslint/no-empty-function, @typescript-eslint/no-unused-vars */

import { Base } from 'discord.js';

export class BaseDatabase extends Base {
  constructor(client, data) {
    super(client);
  }
}
